# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.6

# ── Database ───────────────────────────────────────────────────────────────
motor>=3.5.1
pymongo>=4.8.0

# ── Authentication ─────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
bcrypt>=4.2.0
passlib[bcrypt]>=1.7.4

# ── Image processing ───────────────────────────────────────────────────────
opencv-python>=4.10.0
numpy>=1.26.4
Pillow>=10.4.0

# face-recognition stack (optional)
# On Windows + Python 3.14 use prebuilt dlib and skip source dlib build:
#   c:/python314/python.exe -m pip install dlib-bin "setuptools<81"
#   c:/python314/python.exe -m pip install face-recognition-models face-recognition --no-deps
# Keep this commented to avoid breaking `pip install -r requirements.txt`.
# dlib-bin>=20.0.0
# face-recognition-models>=0.3.0
# face-recognition>=1.3.0
# setuptools<81

# ── Utilities ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.1
python-multipart>=0.0.9
pydantic[email]>=2.8.2
